@{
  Layout = "_Layout";
}
<p>Item index</p>
@if (Model.Count != 0)
{
<h4>No Items have been made</h4>
}
@foreach (var item in Model)
{
    <h4>@item.description</h4>
}

<a href = "/items/new">Add New Items</a>
